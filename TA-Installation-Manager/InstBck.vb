Module InstBck
    Sub InstBckInit(MyDepoPath As String, ResultPathExist As Boolean)
        xtrace_subs("InstBckInit")

        xtrace_sube("InstBckInit")
    End Sub

    Sub InstBackup()
        xtrace_subs("InstBackup")

        xtrace_sube("InstBackup")
    End Sub
End Module
